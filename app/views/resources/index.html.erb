<% content_for :title, "NewsJack resources" %>
<% content_for :meta_description, "Empower your brand with NewsJack — Seamlessly match trends, harness AI, and generate engaging content. Elevate your brand's presence effortlessly" %>

<main>
 <section class="container py-5 mt-5">
    <%= render 'shared/back' %>
    <div>
        <div class="row">
            <div class="col-xl-12 text-center pb-3">
                <div class="section-title service-title">
                  <h1 class="fw-bold text-center m-0 bigger">Resources</h1>
                </div>
            </div>
        </div>
        <div class="row">
          <% @resources.each do |resource| %>
            <div class="col-xl-4 col-lg-4 col-md-6 mb-30">
                <div class="blog-wrapper home-blog-wrapper white-bg">
                    <%# <div class="blog-thumb">
                        <a href="https://bit.ly/2LHKl61">
                            <img src="https://i.ibb.co/HrtN39y/services1.jpg" alt="" />
                        </a>
                    </div> %>
                    <%# <div class="meta-info">
                        <ul>
                            <li class="resources-time">January 08, 2019</li>
                        </ul>
                    </div> %>
                    <div class="blog-content home-blog text-center">
                        <h2 class="blog-title">
                            <%= link_to resource.title, resource_path(resource) %>
                        </h2>
                        <p>
                          <% truncated_content = truncate(strip_tags(resource.content), length: 200) %>
                          <%= markdown(truncated_content) %>
                        </p>
                        <%# <p><%= truncate(resource.content, length: 200) %>
                    </div>
                    <div class="link-box home-blog-link">
                        <h2><%= link_to "Read More", resource_path(resource) %></h2>
                    </div>
                </div>
            </div>
          <% end %>
        </div>
    </div>
</section>
